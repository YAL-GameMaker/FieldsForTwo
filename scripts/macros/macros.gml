#macro delta_time_s (delta_time / 1000000)
/**
	Can be set to:
	0: normal run (default)
	-1: switch to TCP even if Steam is available (menu tests)
	1: TCP, auto-connect, auto-start, a bunch of points (in-game tests)
**/
#macro autotest 0